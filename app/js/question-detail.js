
// const likeButton = document.querySelector('.like-button');
// const likeCount = document.querySelector('.like-count');

// let liked = false; // Флаг для отслеживания состояния лайка

// likeButton.addEventListener('click', () => {
// 	const currentCount = parseInt(likeCount.textContent, 10);

// 	if (!liked) {
// 		// Если лайк не поставлен, добавляем 1
// 		likeCount.textContent = currentCount + 1;
// 		likeButton.classList.add('active')
// 	} else {
// 		// Если лайк уже стоит, убираем 1
// 		likeCount.textContent = currentCount - 1;
// 		likeButton.classList.remove('active')
// 	}

// 	liked = !liked; // Переключаем флаг состояния
// });

